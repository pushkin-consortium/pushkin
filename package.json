{
  "name": "pushkin-cli",
  "version": "0.0.12",
  "description": "A CLI for automating basic Pushkin tasks",
  "main": "build/index.js",
  "files": [
    "build/*"
  ],
  "bin": {
    "pushkin": "build/index.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf build/* && babel src -d build --ignore src/commands/init/initFiles/pushkin,src/commands/generate/generateFiles && mkdir build/commands/init/initFiles && tar -czf build/commands/init/initFiles/pushkin.tar.gz src/commands/init/initFiles/pushkin && cp src/commands/init/initFiles/pushkin.yaml build/commands/init/initFiles && mkdir build/commands/generate/generateFiles && tar -czf build/commands/generate/generateFiles/template.tar.gz src/commands/generate/generateFiles/template"
  },
  "keywords": [
    "pushkin",
    "cli"
  ],
  "author": "L3",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.4.3",
    "@babel/core": "^7.4.3",
    "@babel/preset-env": "^7.4.3",
    "babel-eslint": "^10.0.1",
    "eslint": "^5.16.0"
  },
  "dependencies": {
    "balanced-match": "^1.0.0",
    "command-line-args": "^5.1.1",
    "js-yaml": "^3.13.1",
    "knex": "^0.16.3",
    "pg": "^7.9.0",
    "replace-in-file": "^3.4.4",
    "tar": "^4.4.8",
    "uuid": "^3.3.2",
    "shelljs": "^0.8.3"
  }
}
