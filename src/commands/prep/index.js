import path from 'path';

export default (experimentsDir, coreDir) => {
	// 1. run this for each experiment found in experimentsDir
	// 2. read in the experiment's config file
	// API CONTROLLER:
	// template needs to read in from a folder of controllers with config (attachment) information
	// require each one and attach to the appropriate spot
	// prep needs to copy?
	//
	// WORKER:
	// WEB PAGE:
};
