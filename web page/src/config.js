// Set configurations for experiment
// Options for responseType are "2afc" for a 2-alternative forced choice, "likert" for a likert scale, or "slider" for a 0-100 sliding scale

const experimentConfig = {
    fontColor: "black",
    fontSize: "20px",
    fontFamily: "'Open Sans', 'Arial', sans-serif",
    correctiveFeedback: true,
    responseType: "2afc"
}

export default experimentConfig;