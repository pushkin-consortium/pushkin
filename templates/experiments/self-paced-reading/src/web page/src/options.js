// Set experiment options

const expOptions = {
  fontColor: "black",
  fontSize: "20px",
  fontFamily: "'Open Sans', 'Arial', sans-serif",
  correctiveFeedback: true,
  // correctiveFeedback only relevant if comprehension is set to true
  comprehension: true,
};

export default expOptions;
